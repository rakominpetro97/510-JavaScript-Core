<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <script>
        let x = "1"         //Task 1a
        let y = "2"
        let res = x+y;
        console.log(res);
        console.log("1"+2)

        let res2 ="x+y" 
        console.log(11 >= 11) //Task 1b
        console.log(res2)
        console.log(typeof res2)

        let x = "1"; // Task 1c
        let bln = !!x;
        console.log(bln);
        console.log(typeof bln);


        let x = "20"; // Task 1d
        let = res4 = x;
        console.log(res4);
        console.log(typeof res4);
        
        let number = prompt("Choose a number"); // Task 2
        if (number % 2 === 0 && number > 0) {
            console.log ("Your number is paired positive")
        } else if (number % 7 === 0) {
            console.log("You number is multiple 7");
        } else {
            console.log("It's not a number");
        }
        
         let arr = [];
         arr[0] = 25; // Task 3/1

         let arr = [];
         arr[1] = ["I love my mother"]; // Task 3/2
           
        let arr = [];  // Task 3/3
        arr[2] = [5 + 2 === 7];

        let arr = [];    // Task 3/4
        arr [3] = [null];

       let arr = []; // Task 3/5
       arr [4] = [123456];
       alert(arr);

       let arr = []; // Task 3/6
       arr [5] = []
       let arr = ["123456"];
       console.log[5];
       arr [5] = "654321";
       console.log(arr[5]);

       let arr = [];  // Task 3/7
       arr[6]
       arr [5] = [123456];
       alert(arr);

       let arr = [];   // Task 3/8
       arr[0] = 25;
       alert(arr[0])

       let cities = ["Rome", "Lviv","Warsaw"]; //Task 4
       cities.join(" * ");

       let a = Number(prompt("Set your age")); // 5 Task
       if (a > 18);{
      console.log("You are old enough");
       }
       console.log("You are too young");
       

       

      let a = Number(prompt("Set a value of first side")); // Task 6
      let b = Number(prompt("Set a value of second side"));
      let c = Number(prompt("Set a value of third side"));

      if (a > 0 && b > 0 && c > 0) {
          let p = (a + b + c) / 2;
          let triangleArea = tMah.sqrt(p * (p - a) * (p - b) * (p - c));
          console.log("Triangle area is:" + triangleArea.toFixed(3));
      }
      else {
          alert("Incorrectt data!");
      }
      
      if ((a * a + b * b === c * c) || (b * b + c * c === a * a) || (c * c + a * a === b * b))
      console.log("The triangle is right-angeled")
      
      else {
          console.log("The triangle isn't right-angeled")
      }

       let data = new Date();  // Task 7
       let currentHour = data.getHours();
       if (currentHour >=23 && currentHour <= 5) {
           alert("Good night!");
       }
       else if (currentHour > 5 && currentHour <= 11) {
           alert("Good morning!");
       }
       else if (currentHour > 11 && currentHour <= 17) {
           alert("Good day!");
       }
       else if (currentHour > 17 && currentHour < 23) {
           alert("Good evening");
       }
    </script>
</body>
</html>
