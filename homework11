<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<script>

    // ******************************* Task 1 *******************************

    // document.getElementById("test").innerHTML = "Last";
    // document.querySelector("#test").innerHTML = "Last2";


    // ******************************* Task 2 *******************************

    // let elem = document.getElementsByClassName("image")[0];
    // elem.src = "cat.jpg";        
    // alert(elem.outerHTML);


    // ******************************* Task 3 *******************************

    // var elems = document.querySelectorAll("#text p");

    // for (var i = 0; i < elems.length; i++) {
    //     alert("Selector text " + i + ": " + elems[i].innerHTML);
    // } 


    // ******************************* Task 4 *******************************  

    // var parentEl = document.getElementById("list");
    // var firstEl = parentEl.childNodes[0].innerHTML;
    // var lastEl = parentEl.childNodes[4].innerHTML;
    // var secondtEl = parentEl.childNodes[1].innerHTML;
    // var fourthEl = parentEl.childNodes[3].innerHTML;
    // var thirdtEl = parentEl.childNodes[2].innerHTML;

    // var firstEl = document.getElementById("list").firstChild.innerHTML;
    // var lastEl = document.getElementById("list").lastChild.innerHTML;
    // var secondtEl = document.getElementById("list").firstChild.nextSibling.innerHTML;
    // var fourthEl = document.getElementById("list").lastChild.previousSibling.innerHTML;
    // var thirdtEl = document.getElementById("list").firstChild.nextSibling.nextSibling.innerHTML;

    // alert(firstEl);
    // alert(lastEl);
    // alert(secondtEl);
    // alert(fourthEl);
    // alert(thirdtEl);

    // ******************************* Task 5 *******************************

    // document.body.children[0].style.backgroundColor = "lightgreen";
    // document.getElementById("myDiv").children[0].style.fontWeight = "700";
    // document.getElementById("myDiv").children[1].style.color = "red";
    // document.getElementById("myDiv").children[2].style.textDecoration = "underline";
    // document.getElementById("myDiv").children[3].style.fontStyle = "italic";
    // document.getElementById("myList").style.listStyle = "none";
    // document.getElementById("myList").style.display = "flex";
    // document.getElementsByTagName("span")[0].style.display = "none";


    // ******************************* Task 6 *******************************

    // var data1 = prompt("Enter your first message", "");        
    // var data2 = prompt("Enter your second message", "");
    // var inp1 = document.getElementById("input1");
    // var inp2 = document.getElementById("input2");
    // inp1.value = data1;
    // inp2.value = data2;

    // var input1Value = inp1.value;
    // var input2Value = inp2.value;
    // inp1.value = input2Value;
    // inp2.value = input1Value;

    // ******************************* Task 7 *******************************
    const x = 100;
    document.body.innerHTML =
        `<main class="mainClass check item">
          <div id="myDiv">${x}
              <p>First paragraph</p>           
          </div>
         </main>`;

</script>
</body>
</html>
